package com.kumar.observer.example;

// the DisplayElement interface just include one method,
// display, that we will call when the display element needs to be displayed
public interface DisplayElement {
    public  void  display();
}
